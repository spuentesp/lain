version = 1

[[agent]]
id = "claude"
display_name = "Claude Code"
binary = "claude"
detect_paths = ["~/.claude"]
config_user = "~/.claude/settings.json"
config_project = ".claude/settings.json"
config_format = "json"
mcp_section = "mcpServers"
mcp_name = "lain"
transport = "stdio"
command = "/home/sebastian/.local/lain/lain"
default_args = ["--workspace", "{{workspace}}", "--transport", "stdio", "--embedding-model", "/home/sebastian/.local/lain/models/all-MiniLM-L6-v2.onnx"]
headless_probe = ["claude", "--print", "--mcp-config", "{{config_path}}", "list your tools"]
format = "json"

[[agent]]
id = "kimi"
display_name = "Kimi Code"
binary = "kimi"
detect_paths = ["~/.kimi-code/plugins/managed/lain"]
config_user = "~/.kimi-code/plugins/managed/lain/kimi.plugin.json"
config_project = ".kimi-code/plugins/lain/kimi.plugin.json"
config_format = "kimi-plugin"
mcp_section = "mcpServers"
mcp_name = "lain"
transport = "stdio"
command = "/home/sebastian/.local/lain/lain"
default_args = ["--workspace", "{{workspace}}", "--transport", "stdio", "--embedding-model", "/home/sebastian/.local/lain/models/all-MiniLM-L6-v2.onnx"]
headless_probe = ["kimi", "-p", "list your tools"]
format = "json"

[[agent]]
id = "cursor"
display_name = "Cursor"
binary = "cursor"
detect_paths = ["~/.cursor"]
config_user = "~/.cursor/mcp.json"
config_project = ".cursor/mcp.json"
config_format = "json"
mcp_section = "mcpServers"
mcp_name = "lain"
transport = "http"
command = "/home/sebastian/.local/lain/lain"
default_args = ["--workspace", "{{workspace}}", "--transport", "stdio", "--embedding-model", "/home/sebastian/.local/lain/models/all-MiniLM-L6-v2.onnx"]
headless_probe = []
format = "http"

[[agent]]
id = "continue"
display_name = "Continue.dev"
binary = "continue-cli"
detect_paths = ["~/.continue"]
config_user = "~/.continue/config.json"
config_project = ".continue/config.json"
config_format = "continue"
mcp_section = "mcpServers"
mcp_name = "lain"
transport = "http"
command = "/home/sebastian/.local/lain/lain"
default_args = ["--workspace", "{{workspace}}", "--transport", "stdio", "--embedding-model", "/home/sebastian/.local/lain/models/all-MiniLM-L6-v2.onnx"]
headless_probe = []
format = "http"

[[agent]]
id = "antigravity"
display_name = "Antigravity CLI (agy)"
binary = "agy"
detect_paths = ["~/.gemini/antigravity-cli", "~/.gemini"]
# `agy` reads MCP servers from `~/.gemini/config/mcp_config.json` (a separate
# file from `settings.json`). The adapter writes there.
config_user = "~/.gemini/config/mcp_config.json"
config_project = ".gemini/config/mcp_config.json"
config_format = "gemini-mcp-config"
mcp_section = "mcpServers"
mcp_name = "lain"
transport = "http"
command = "/home/sebastian/.local/lain/lain"
default_args = ["--workspace", "{{workspace}}", "--transport", "stdio", "--embedding-model", "/home/sebastian/.local/lain/models/all-MiniLM-L6-v2.onnx"]
headless_probe = ["agy", "--print", "list your MCP tools"]
format = "http"

[[agent]]
id = "cline"
display_name = "Cline"
binary = "cline"
detect_paths = ["~/.cline"]
config_user = "~/.cline/mcp.json"
config_project = ".cline/mcp.json"
config_format = "json"
mcp_section = "mcpServers"
mcp_name = "lain"
transport = "http"
command = "/home/sebastian/.local/lain/lain"
default_args = ["--workspace", "{{workspace}}", "--transport", "stdio", "--embedding-model", "/home/sebastian/.local/lain/models/all-MiniLM-L6-v2.onnx"]
headless_probe = []
format = "http"

[[agent]]
id = "codex"
display_name = "Codex"
binary = "codex"
detect_paths = ["~/.codex"]
config_user = "~/.codex/mcp.json"
config_project = ".codex/mcp.json"
config_format = "json"
mcp_section = "mcpServers"
mcp_name = "lain"
transport = "http"
command = "/home/sebastian/.local/lain/lain"
default_args = ["--workspace", "{{workspace}}", "--transport", "stdio", "--embedding-model", "/home/sebastian/.local/lain/models/all-MiniLM-L6-v2.onnx"]
headless_probe = ["codex", "--quiet"]
format = "http"

[[agent]]
id = "omp"
display_name = "OMP"
binary = "omp"
detect_paths = ["~/.omp"]
config_user = "~/.omp/mcp.json"
config_project = ".omp/mcp.json"
config_format = "json"
mcp_section = "mcpServers"
mcp_name = "lain"
transport = "http"
command = "/home/sebastian/.local/lain/lain"
default_args = ["--workspace", "{{workspace}}", "--transport", "stdio", "--embedding-model", "/home/sebastian/.local/lain/models/all-MiniLM-L6-v2.onnx"]
headless_probe = ["omp", "--headless"]
format = "http"

[[agent]]
# Windsurf ships only as a full IDE; no headless CLI. The install loop can still
# write this config and `lain agents verify` will mark it `not installed`
# until a Windsurf IDE on this host creates the file. The Windsurf row is
# kept so the manifest stays a single source of truth and a future Windsurf
# CLI does not require a manifest change.
id = "windsurf"
display_name = "Windsurf (no headless CLI)"
binary = "windsurf"
detect_paths = ["~/.windsurf"]
config_user = "~/.windsurf/mcp.json"
config_project = ".windsurf/mcp.json"
config_format = "json"
mcp_section = "mcpServers"
mcp_name = "lain"
transport = "http"
command = "/home/sebastian/.local/lain/lain"
default_args = ["--workspace", "{{workspace}}", "--transport", "stdio", "--embedding-model", "/home/sebastian/.local/lain/models/all-MiniLM-L6-v2.onnx"]
headless_probe = []
format = "http"

[[agent]]
id = "opencode"
display_name = "OpenCode"
binary = "opencode"
detect_paths = ["~/.config/opencode"]
config_user = "~/.config/opencode/opencode.json"
config_project = "opencode.json"
config_format = "jsonc"
mcp_section = "mcp"
mcp_name = "lain"
transport = "stdio"
command = "lain"
default_args = []
headless_probe = ["opencode", "--version"]

[[agent]]
id = "copilot"
display_name = "GitHub Copilot in VS Code"
binary = "code"
detect_paths = ["~/.config/Code", "~/.vscode"]
config_user = "~/.copilot/mcp-config.json"
config_project = ".vscode/mcp.json"
config_format = "jsonc"
mcp_section = "servers"
mcp_name = "lain"
transport = "stdio"
command = "lain"
default_args = []
headless_probe = ["code", "--version"]