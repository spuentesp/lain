//! Placeholder for the federation orchestrator. The real `FederatedIndex`
//! is implemented in Task 10. This stub exists so that Task 5's
//! `LainServer::with_federation(...)` signature can resolve at compile time;
//! the body is `todo!()` and the type is never instantiated.
#![allow(dead_code)]

/// Federation orchestrator. Task 10 fills in the fields and methods.
pub struct FederatedIndex;
